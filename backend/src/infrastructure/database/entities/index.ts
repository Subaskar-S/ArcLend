/**
 * Global database entities export file.
 * Specific module entities (like Markets, Users) are kept within their respective modules
 * per Clean Architecture principles, but can also be exported here for centralized access
 * in TypeORM configuration.
 */
export * from '../../../modules/markets/entities/market.entity';
// export * from '../../../modules/users/entities/user.entity';
